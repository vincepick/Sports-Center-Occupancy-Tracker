This project is still being reined, below are some of the plans for future updates.

Add ability to read from command line on which DF to read rather than having it hardcoded in generateModel.py

you can enter something as a command line parameter, then can run a bash file to run all of your code using that paramter and it automatically generates a full dataset
